services:
  frontend:
    build:
      context: ../frontend
    ports:
      - "3000:80"
    depends_on:
      - backend-py
      - ml
      - route-go

  backend-py:
    build:
      context: ../backend-py
    ports:
      - "8000:8000"

  ml:
    build:
      context: ../ml
    ports:
      - "8001:8001"

  route-go:
    build:
      context: ../route-go
    ports:
      - "8080:8080"